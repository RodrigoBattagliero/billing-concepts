<form method="post" action="{{ path('app_unit_measurement_delete', {'id': unit_measurement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit_measurement.id) }}">
    <button class="btn btn-danger btn-sm">Eliminar</button>
</form>
