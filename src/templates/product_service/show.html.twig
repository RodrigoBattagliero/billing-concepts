{% extends 'base.html.twig' %}

{% block title %}ProductService{% endblock %}

{% block body %}
    <h1>ProductService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_service.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ product_service.type }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ product_service.code }}</td>
            </tr>
            <tr>
                <th>ProductService</th>
                <td>{{ product_service.productService }}</td>
            </tr>
            <tr>
                <th>GrossPrice</th>
                <td>{{ product_service.grossPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_service_index') }}">back to list</a>

    <a href="{{ path('app_product_service_edit', {'id': product_service.id}) }}">edit</a>

    {{ include('product_service/_delete_form.html.twig') }}
{% endblock %}
