{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}" class="btn btn-primary btn-sm">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-info btn-sm">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
